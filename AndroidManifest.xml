<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Inaruhusu NjiaFix kuamuru Termux itekeleze amri (adb/fastboot) moja kwa moja.
         Mtumiaji ata-grant hii mara moja tu (dangerous permission ya Termux). -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NjiaFix"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name=".ui.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.PhoneActivity" android:parentActivityName=".ui.MainActivity" />
        <activity android:name=".ui.PaymentActivity" android:parentActivityName=".ui.PhoneActivity" />
        <activity android:name=".ui.UnlockActivity" android:parentActivityName=".ui.PhoneActivity" />
        <activity android:name=".ui.ToolsActivity" android:parentActivityName=".ui.PhoneActivity" />
        <activity android:name=".ui.OtherDevicesActivity" android:parentActivityName=".ui.MainActivity" />
        <activity android:name=".ui.SettingsActivity" android:parentActivityName=".ui.MainActivity" />
    </application>
</manifest>
